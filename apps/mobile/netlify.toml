[build]
  command   = "npm run build:web"
  publish   = "dist"

[build.environment]
  NODE_VERSION = "20"

# Con output=static, expo-router genera archivos por ruta
# El fallback a index.html maneja rutas no encontradas
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
