[build]
  command   = "npm run build:web"
  publish   = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback: todas las rutas sirven index.html (necesario para expo-router)
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
