<mxfile host="app.diagrams.net" modified="2026-05-10T00:00:00.000Z" agent="Kiro" version="21.0.0">
  <diagram name="Arquitectura Vademécum" id="arquitectura">
    <mxGraphModel dx="1422" dy="762" grid="1" gridSize="10" guides="1" tooltips="1" connect="1" arrows="1" fold="1" page="1" pageScale="1" pageWidth="1654" pageHeight="1169" math="0" shadow="0">
      <root>
        <mxCell id="0" />
        <mxCell id="1" parent="0" />

        <!-- ═══════════════════════════════════════════════════════ -->
        <!-- TÍTULO -->
        <!-- ═══════════════════════════════════════════════════════ -->
        <mxCell id="title" value="Arquitectura — Vademécum (drug-medicine-lookup)" style="text;html=1;strokeColor=none;fillColor=none;align=center;verticalAlign=middle;whiteSpace=wrap;rounded=0;fontSize=18;fontStyle=1;" vertex="1" parent="1">
          <mxGeometry x="400" y="30" width="760" height="40" as="geometry" />
        </mxCell>

        <!-- ═══════════════════════════════════════════════════════ -->
        <!-- ZONA: DESARROLLADOR -->
        <!-- ═══════════════════════════════════════════════════════ -->
        <mxCell id="zone-dev" value="Máquina del desarrollador" style="swimlane;startSize=30;fillColor=#f5f5f5;strokeColor=#666666;fontColor=#333333;fontStyle=1;fontSize=11;" vertex="1" parent="1">
          <mxGeometry x="40" y="100" width="340" height="280" as="geometry" />
        </mxCell>

        <!-- Kiro IDE -->
        <mxCell id="kiro" value="Kiro IDE&#xa;(editor + agente IA)" style="rounded=1;whiteSpace=wrap;html=1;fillColor=#dae8fc;strokeColor=#6c8ebf;fontSize=11;fontStyle=1;" vertex="1" parent="zone-dev">
          <mxGeometry x="80" y="50" width="180" height="60" as="geometry" />
        </mxCell>

        <!-- Monorepo -->
        <mxCell id="monorepo" value="Monorepo (npm workspaces)&#xa;packages/shared&#xa;apps/api&#xa;apps/mobile" style="rounded=1;whiteSpace=wrap;html=1;fillColor=#fff2cc;strokeColor=#d6b656;fontSize=10;" vertex="1" parent="zone-dev">
          <mxGeometry x="60" y="150" width="220" height="80" as="geometry" />
        </mxCell>

        <!-- Git -->
        <mxCell id="git" value="Git (local)" style="rounded=1;whiteSpace=wrap;html=1;fillColor=#f8cecc;strokeColor=#b85450;fontSize=11;" vertex="1" parent="zone-dev">
          <mxGeometry x="80" y="260" width="180" height="50" as="geometry" />
        </mxCell>

        <!-- ═══════════════════════════════════════════════════════ -->
        <!-- ZONA: GITHUB -->
        <!-- ═══════════════════════════════════════════════════════ -->
        <mxCell id="zone-github" value="GitHub" style="swimlane;startSize=30;fillColor=#f5f5f5;strokeColor=#666666;fontColor=#333333;fontStyle=1;fontSize=11;" vertex="1" parent="1">
          <mxGeometry x="460" y="100" width="220" height="130" as="geometry" />
        </mxCell>

        <mxCell id="github-repo" value="Gus3103/vademecum&#xa;(repositorio)" style="rounded=1;whiteSpace=wrap;html=1;fillColor=#f8cecc;strokeColor=#b85450;fontSize=11;" vertex="1" parent="zone-github">
          <mxGeometry x="30" y="50" width="160" height="60" as="geometry" />
        </mxCell>

        <!-- ═══════════════════════════════════════════════════════ -->
        <!-- ZONA: RAILWAY (backend) -->
        <!-- ═══════════════════════════════════════════════════════ -->
        <mxCell id="zone-railway" value="Railway (backend)" style="swimlane;startSize=30;fillColor=#f5f5f5;strokeColor=#666666;fontColor=#333333;fontStyle=1;fontSize=11;" vertex="1" parent="1">
          <mxGeometry x="460" y="290" width="340" height="340" as="geometry" />
        </mxCell>

        <!-- API Node.js -->
        <mxCell id="api" value="API REST&#xa;Node.js + Express + TypeScript&#xa;apps/api&#xa;puerto: $PORT" style="rounded=1;whiteSpace=wrap;html=1;fillColor=#d5e8d4;strokeColor=#82b366;fontSize=10;fontStyle=1;" vertex="1" parent="zone-railway">
          <mxGeometry x="50" y="50" width="240" height="80" as="geometry" />
        </mxCell>

        <!-- Rutas -->
        <mxCell id="routes" value="GET /api/v1/medicines/search&#xa;GET /api/v1/medicines/suggestions&#xa;GET /api/v1/medicines/:id&#xa;GET /api/v1/medicines/:id/prospect&#xa;POST /api/v1/interactions/check" style="rounded=1;whiteSpace=wrap;html=1;fillColor=#d5e8d4;strokeColor=#82b366;fontSize=9;align=left;" vertex="1" parent="zone-railway">
          <mxGeometry x="50" y="150" width="240" height="90" as="geometry" />
        </mxCell>

        <!-- PostgreSQL -->
        <mxCell id="postgres" value="PostgreSQL&#xa;(Railway managed)&#xa;&#xa;active_ingredients&#xa;medicines&#xa;medicine_ingredients&#xa;prospects&#xa;drug_interactions" style="shape=cylinder3;whiteSpace=wrap;html=1;fillColor=#dae8fc;strokeColor=#6c8ebf;fontSize=9;" vertex="1" parent="zone-railway">
          <mxGeometry x="50" y="260" width="240" height="60" as="geometry" />
        </mxCell>

        <!-- ═══════════════════════════════════════════════════════ -->
        <!-- ZONA: NETLIFY (frontend) -->
        <!-- ═══════════════════════════════════════════════════════ -->
        <mxCell id="zone-netlify" value="Netlify (frontend estático)" style="swimlane;startSize=30;fillColor=#f5f5f5;strokeColor=#666666;fontColor=#333333;fontStyle=1;fontSize=11;" vertex="1" parent="1">
          <mxGeometry x="880" y="100" width="320" height="340" as="geometry" />
        </mxCell>

        <!-- React Native Web -->
        <mxCell id="rnweb" value="React Native Web&#xa;(Expo export -p web)&#xa;apps/mobile → dist/" style="rounded=1;whiteSpace=wrap;html=1;fillColor=#e1d5e7;strokeColor=#9673a6;fontSize=10;fontStyle=1;" vertex="1" parent="zone-netlify">
          <mxGeometry x="60" y="50" width="200" height="70" as="geometry" />
        </mxCell>

        <!-- Pantallas -->
        <mxCell id="screens" value="SearchScreen&#xa;ResultsScreen&#xa;ProspectScreen&#xa;InteractionsScreen&#xa;HistoryScreen" style="rounded=1;whiteSpace=wrap;html=1;fillColor=#e1d5e7;strokeColor=#9673a6;fontSize=9;align=left;" vertex="1" parent="zone-netlify">
          <mxGeometry x="60" y="140" width="200" height="90" as="geometry" />
        </mxCell>

        <!-- Zustand -->
        <mxCell id="zustand" value="Estado: Zustand&#xa;searchStore / filterStore&#xa;interactionStore" style="rounded=1;whiteSpace=wrap;html=1;fillColor=#e1d5e7;strokeColor=#9673a6;fontSize=9;" vertex="1" parent="zone-netlify">
          <mxGeometry x="60" y="250" width="200" height="60" as="geometry" />
        </mxCell>

        <!-- ═══════════════════════════════════════════════════════ -->
        <!-- ZONA: USUARIO / DISPOSITIVO -->
        <!-- ═══════════════════════════════════════════════════════ -->
        <mxCell id="zone-user" value="Usuario" style="swimlane;startSize=30;fillColor=#f5f5f5;strokeColor=#666666;fontColor=#333333;fontStyle=1;fontSize=11;" vertex="1" parent="1">
          <mxGeometry x="1280" y="100" width="280" height="340" as="geometry" />
        </mxCell>

        <!-- Navegador web -->
        <mxCell id="browser" value="Navegador Web&#xa;Chrome / Firefox / Safari / Edge&#xa;(Req. 7.1)" style="shape=mxgraph.mockup.containers.browser;whiteSpace=wrap;html=1;fillColor=#fff2cc;strokeColor=#d6b656;fontSize=10;" vertex="1" parent="zone-user">
          <mxGeometry x="40" y="50" width="200" height="100" as="geometry" />
        </mxCell>

        <!-- Celular -->
        <mxCell id="mobile" value="Celular&#xa;iOS 15+ / Android 10+&#xa;(Expo Go o build)&#xa;(Req. 7.2)" style="shape=mxgraph.android.phone2;whiteSpace=wrap;html=1;fillColor=#fff2cc;strokeColor=#d6b656;fontSize=10;" vertex="1" parent="zone-user">
          <mxGeometry x="40" y="190" width="200" height="110" as="geometry" />
        </mxCell>

        <!-- ═══════════════════════════════════════════════════════ -->
        <!-- ZONA: PAQUETE COMPARTIDO -->
        <!-- ═══════════════════════════════════════════════════════ -->
        <mxCell id="zone-shared" value="packages/shared (TypeScript)" style="swimlane;startSize=30;fillColor=#fff2cc;strokeColor=#d6b656;fontColor=#333333;fontStyle=1;fontSize=11;" vertex="1" parent="1">
          <mxGeometry x="460" y="680" width="740" height="120" as="geometry" />
        </mxCell>

        <mxCell id="shared-types" value="Tipos compartidos&#xa;Medicine, Prospect, DrugInteraction&#xa;SearchResult, InteractionResult, HistoryEntry, FilterState, ApiError" style="rounded=1;whiteSpace=wrap;html=1;fillColor=#fff2cc;strokeColor=#d6b656;fontSize=9;" vertex="1" parent="zone-shared">
          <mxGeometry x="30" y="40" width="300" height="60" as="geometry" />
        </mxCell>

        <mxCell id="shared-utils" value="normalizeText()&#xa;(tildes + mayúsculas)&#xa;Req. 1.4" style="rounded=1;whiteSpace=wrap;html=1;fillColor=#fff2cc;strokeColor=#d6b656;fontSize=9;" vertex="1" parent="zone-shared">
          <mxGeometry x="410" y="40" width="160" height="60" as="geometry" />
        </mxCell>

        <mxCell id="shared-tests" value="Tests: Vitest + fast-check&#xa;39 tests pasando" style="rounded=1;whiteSpace=wrap;html=1;fillColor=#d5e8d4;strokeColor=#82b366;fontSize=9;" vertex="1" parent="zone-shared">
          <mxGeometry x="590" y="40" width="120" height="60" as="geometry" />
        </mxCell>

        <!-- ═══════════════════════════════════════════════════════ -->
        <!-- FLECHAS -->
        <!-- ═══════════════════════════════════════════════════════ -->

        <!-- Kiro edita monorepo -->
        <mxCell id="e1" style="edgeStyle=orthogonalEdgeStyle;" edge="1" source="kiro" target="monorepo" parent="zone-dev">
          <mxGeometry relative="1" as="geometry" />
        </mxCell>

        <!-- Monorepo → git local -->
        <mxCell id="e2" style="edgeStyle=orthogonalEdgeStyle;" edge="1" source="monorepo" target="git" parent="zone-dev">
          <mxGeometry relative="1" as="geometry" />
        </mxCell>

        <!-- git push → GitHub -->
        <mxCell id="e3" value="git push" style="edgeStyle=orthogonalEdgeStyle;dashed=0;exitX=1;exitY=0.5;exitDx=0;exitDy=0;" edge="1" source="git" target="github-repo" parent="1">
          <mxGeometry relative="1" as="geometry" />
        </mxCell>

        <!-- GitHub → Railway (auto deploy) -->
        <mxCell id="e4" value="auto deploy" style="edgeStyle=orthogonalEdgeStyle;dashed=1;exitX=0.5;exitY=1;exitDx=0;exitDy=0;" edge="1" source="github-repo" target="api" parent="1">
          <mxGeometry relative="1" as="geometry" />
        </mxCell>

        <!-- GitHub → Netlify (auto deploy) -->
        <mxCell id="e5" value="auto deploy" style="edgeStyle=orthogonalEdgeStyle;dashed=1;" edge="1" source="github-repo" target="rnweb" parent="1">
          <mxGeometry relative="1" as="geometry" />
        </mxCell>

        <!-- API → PostgreSQL -->
        <mxCell id="e6" value="SQL queries&#xa;(pg_trgm)" style="edgeStyle=orthogonalEdgeStyle;" edge="1" source="api" target="postgres" parent="zone-railway">
          <mxGeometry relative="1" as="geometry" />
        </mxCell>

        <!-- API → rutas -->
        <mxCell id="e7" style="edgeStyle=orthogonalEdgeStyle;" edge="1" source="api" target="routes" parent="zone-railway">
          <mxGeometry relative="1" as="geometry" />
        </mxCell>

        <!-- Frontend → API (HTTPS) -->
        <mxCell id="e8" value="HTTPS / REST&#xa;EXPO_PUBLIC_API_URL" style="edgeStyle=orthogonalEdgeStyle;dashed=0;" edge="1" source="rnweb" target="routes" parent="1">
          <mxGeometry relative="1" as="geometry" />
        </mxCell>

        <!-- Netlify → Browser -->
        <mxCell id="e9" value="sirve HTML/JS/CSS" style="edgeStyle=orthogonalEdgeStyle;" edge="1" source="rnweb" target="browser" parent="1">
          <mxGeometry relative="1" as="geometry" />
        </mxCell>

        <!-- Expo Go → mobile -->
        <mxCell id="e10" value="Expo Go / build" style="edgeStyle=orthogonalEdgeStyle;" edge="1" source="rnweb" target="mobile" parent="1">
          <mxGeometry relative="1" as="geometry" />
        </mxCell>

        <!-- shared → API -->
        <mxCell id="e11" value="npm workspace" style="edgeStyle=orthogonalEdgeStyle;dashed=1;exitX=0.5;exitY=0;exitDx=0;exitDy=0;" edge="1" source="shared-types" target="api" parent="1">
          <mxGeometry relative="1" as="geometry" />
        </mxCell>

        <!-- shared → mobile -->
        <mxCell id="e12" value="npm workspace" style="edgeStyle=orthogonalEdgeStyle;dashed=1;exitX=0.5;exitY=0;exitDx=0;exitDy=0;" edge="1" source="shared-types" target="rnweb" parent="1">
          <mxGeometry relative="1" as="geometry" />
        </mxCell>

        <!-- normalizeText → API -->
        <mxCell id="e13" style="edgeStyle=orthogonalEdgeStyle;dashed=1;exitX=0.5;exitY=0;exitDx=0;exitDy=0;" edge="1" source="shared-utils" target="api" parent="1">
          <mxGeometry relative="1" as="geometry" />
        </mxCell>

        <!-- normalizeText → mobile -->
        <mxCell id="e14" style="edgeStyle=orthogonalEdgeStyle;dashed=1;exitX=0.5;exitY=0;exitDx=0;exitDy=0;" edge="1" source="shared-utils" target="rnweb" parent="1">
          <mxGeometry relative="1" as="geometry" />
        </mxCell>

        <!-- AsyncStorage label -->
        <mxCell id="async-label" value="AsyncStorage (local)&#xa;Historial 20 entradas&#xa;Req. 6.1–6.5" style="rounded=1;whiteSpace=wrap;html=1;fillColor=#e1d5e7;strokeColor=#9673a6;fontSize=9;" vertex="1" parent="1">
          <mxGeometry x="1120" y="490" width="160" height="60" as="geometry" />
        </mxCell>

        <mxCell id="e15" style="edgeStyle=orthogonalEdgeStyle;dashed=1;" edge="1" source="zustand" target="async-label" parent="1">
          <mxGeometry relative="1" as="geometry" />
        </mxCell>

      </root>
    </mxGraphModel>
  </diagram>
</mxfile>
